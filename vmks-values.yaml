# Минимум поверх дефолтов чарта: только ingress для Grafana.
# Без vmcluster и без кастомного Alertmanager с внешними Secret.
grafana:
  ingress:
    ingressClassName: nginx
    enabled: true
    hosts:
      - grafana.apatsev.org.ru
